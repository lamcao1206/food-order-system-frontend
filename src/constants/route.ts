export enum RouteId {
  AUTH = 'auth',
  PUBLIC = 'public',
  FOOD = 'food',
  ORDER = 'order',
  HISTORY= 'history',
  PUBLIC_ROOT = 'public-root',
  PRIVATE_ROOT = 'private-root'
}

export enum CategoryIntro {
  
}