export enum RouteId {
  AUTH = 'auth',
  PUBLIC = 'public',
  FOOD = 'food',
  ORDER = 'order',
  HISTORY= 'history',
  RESTAURANT = 'restaurant',
  PUBLIC_ROOT = 'public-root',
  PRIVATE_ROOT = 'private-root',
  RESTAURANT_ROOT = 'restaurant-root'
}

export enum CategoryIntro {
  
}